import React, { useState } from 'react';
const SectionInfo = ({ SectionInfo }) => {
  
    return (
      <div>
        <h1>{SectionInfo}</h1>
      </div>
    );
  };
  
  export default SectionInfo;